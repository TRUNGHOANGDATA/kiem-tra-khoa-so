[pytest]
pythonpath = .
testpaths = tests
# Suite phải sạch dưới -W error: cảnh báo pandas (dtype, downcast, parse ngày) từng
# là dấu hiệu sớm của đúng những lỗi im lặng đã phải sửa trên nhánh này.
filterwarnings = error
